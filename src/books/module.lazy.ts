/** The Books card, hover card and Now band answers, behind the chunk that draws them. See `vg/module.lazy.ts`. */
export { default as CardMediaImage, BookHoverCard as HoverCard } from "./CardMediaImage";
export { now } from "./now";
